spring:
  application:
    name: gateway
  cloud:
    consul:
      host: ${CONSUL_HOST}
      port: ${CONSUL_PORT}
      config:
        enabled: true
        prefix: microservices
        profileSeparator: ','
        format: YAML
        watch:
          enabled: true
          delay: 5000