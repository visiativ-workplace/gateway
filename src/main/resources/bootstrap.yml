spring:
  application:
    name: gateway
  cloud:
    consul:
      host: ${env.CONSUL_HOST}
      port: ${env.CONSUL_PORT}
      config:
        enabled: true
        prefix: microservices
        profileSeparator: ','
        format: YAML
        watch:
          enabled: true
          delay: 5000