spring:
  application:
    name: gateway
  cloud:
    consul:
      config:
        enabled: true
        prefix: microservices
        profileSeparator: ','
        format: YAML
        watch:
          enabled: true
          delay: 5000