spring:
  application:
    name: gateway
  cloud:
    consul:
      host: ${CONSUL_HOST:consul}
      port: ${CONSUL_PORT:8500}
      config:
        enabled: true
        prefix: microservices
        profileSeparator: ','
        format: YAML
        watch:
          enabled: true
          delay: 5000